# Mechanism tests

S1 uses `research/results/tt-*.json`: targeting the threshold changes certified volume by only
`+0.000425` with a confidence interval crossing zero and worsens regret by `+0.0301`.
This bounds the mechanism claim and prevents targeting complexity from being oversold.
