# Reviewer-facing checks

The main defenses are executable rather than rhetorical: one-process DC parity supersedes the
cross-run LC estimate; C3 uses the frozen LOFO analyser rather than stale prose; Hill remains
in the five-family result; and every certification statement reports answered and contained
counts. Run `software/scripts/verify_conclusions.py` for the guarded headline values.
