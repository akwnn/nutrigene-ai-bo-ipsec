# Handoff — Joseph round-sweep results (ported to manufacturing `main`)

**Source:** `origin/kr-effective-resolution` @ `07ae40e` (Joseph's machine HEAD was `3bab48f` at
handoff write; final commit adds this doc on his branch).

**Manufacturing worktree:** `/Users/alanakwan/Personal Projects/nutrigene-ai-bo-ipsec/.worktrees/spade-campaign`

Joseph's original paths (`/Users/jy/BO/nutrigene-ai-bo-ipsec`) are preserved in git history on his
branch; use the manufacturing worktree for all commands below.

---

## 1. LC confirmatory (not yet run here)

Joseph had five LC background jobs in flight at handoff. **LC results are not on his branch yet**
(`results/lc-*.json` absent). Relaunch from this worktree when scalar B3 gate allows compute:

```bash
cd "/Users/alanakwan/Personal Projects/nutrigene-ai-bo-ipsec/.worktrees/spade-campaign"
for f in hill ackley hartmann6 levy rosenbrock; do
  nohup .venv/bin/python -u scripts/run_lc_confirmatory.py \
      --seeds 25 --families "$f" --out "results/lc-$f.json" > "/tmp/lc_$f.log" 2>&1 &
done
```

Checkpoints every 2 jobs to `results/lc-<family>.json`; seed-major loop; partial files are analysable.

---

## 2. Confirmed findings (committed on Joseph branch, ported LA/LB JSON)

| finding | evidence |
|---|---|
| **SPADE at 5 rounds = qLogNEI at 10 rounds on regret** | -0.0098, CI [-0.0539, +0.0298], p=0.705, within SESOI 0.02 |
| **SPADE certifies at 5 rounds; qLogNEI needs 10** | LB 0.9342, 27.5% answered, no inflation vs no attainable certificate |
| **SPADE beats qLogNEI on certified volume at matched rounds** | R=3: +0.001000, CI [+0.000413, +0.001769], p=0.0004 |
| **SUR is NOT an improvement** | `results/probe-sur-vs-straddle.json` — 1 win, 1 loss, 4 ties |

Full context: `docs/SPADE-ROUND-SWEEP-SPEC.md`, `results/la-*.json`, `results/lb-*.json`.

---

## 3. Manufacturing gate (separate track)

Scalar manufacturing B3 gate (`digest 2ef1875c…`) runs on hill+levy keys 0–15 via
`~/spade-ops/spade_gate_then_full_watchdog.sh`. Round-sweep wins do **not** substitute for that
gate. See `.planning/SPADE-SCALAR-RECOVERY-DIAGNOSIS.md`.

---

## 4. File map (ported this integration)

| topic | spec | data | script |
|---|---|---|---|
| round sweep, headline | `SPADE-ROUND-SWEEP-SPEC.md` | `results/lb-*.json` | `run_lb_round_sweep.py` |
| round-matched benchmark | `SPADE-ROUND-MATCHED-SPEC.md` | `results/la-*.json` | `run_la_round_matched.py` |
| LC confirmatory | — | `results/lc-*.json` (pending) | `run_lc_confirmatory.py` |
| SUR vs straddle | — | `results/probe-sur-vs-straddle.json` | `bench_certificate.py` |

Integration ledger: `docs/superpowers/specs/2026-08-28-spade-joseph-integration-manifest.md`.
